name: flet_ble_bridge
description: Flet BLE bridge service (flutter_blue_plus)
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flet: ^0.84.0
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.0

dev_dependencies:
  flutter_lints: ^3.0.0
